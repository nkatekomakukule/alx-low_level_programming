#include "main.h)

/**
 * _abs - compute the absolute value of an interger
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		retun (n);
	}
	return (-n);
}
	
